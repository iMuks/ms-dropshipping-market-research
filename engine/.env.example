# Copy this file to .env and fill in your keys.
# Never commit .env — it's in .gitignore.

# --- Anthropic (Claude Agent SDK) ---
ANTHROPIC_API_KEY=sk-ant-...

# --- Langfuse (self-hosted, default docker compose ports) ---
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...

# --- DataForSEO (optional in v0; only used if WebSearch is insufficient) ---
DATAFORSEO_LOGIN=
DATAFORSEO_PASSWORD=

# --- Reddit (PRAW, read-only) ---
# Create a "script" type app at https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=niche-research/0.1 by your_reddit_username

# --- Apify (marketplace scrapers + Facebook community access) ---
# Sign up at apify.com; actors are pay-per-run, ~$0.25–$1 per scrape.
# Facebook actors require a logged-in cookie session (FB_COOKIES_JSON below).
APIFY_TOKEN=
# Marketplace actor IDs:
APIFY_ACTOR_AMAZON=junglee~amazon-crawler
APIFY_ACTOR_TEMU=epctex~temu-scraper
APIFY_ACTOR_SHEIN=epctex~shein-scraper
APIFY_ACTOR_ALIEXPRESS=epctex~aliexpress-scraper
# Facebook actor IDs (community-needs):
APIFY_ACTOR_FB_GROUP=apify~facebook-groups-scraper
APIFY_ACTOR_FB_PAGE=apify~facebook-pages-scraper
APIFY_ACTOR_FB_POST_COMMENTS=apify~facebook-post-comments-scraper
# Cookie JSON for FB Group access (export from a logged-in browser).
# Use a research-only account, NEVER your main FB account.
# Path to a JSON file containing serialized cookies. Leave empty to skip FB.
APIFY_FB_COOKIES_FILE=

# --- Engine config ---
COST_CAP_USD_PER_BRIEF=5.0
ORCHESTRATOR_MODEL=claude-opus-4-7
SPECIALIST_MODEL=claude-sonnet-4-6
REVIEWER_MODEL=claude-opus-4-7
